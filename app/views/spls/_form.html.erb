<%= form_for(@spl) do |f| %>
  <% if @spl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spl.errors.count, "error") %> prohibited this spl from being saved:</h2>

      <ul>
      <% @spl.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
