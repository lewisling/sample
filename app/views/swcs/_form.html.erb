<%= form_for(@swc) do |f| %>
  <% if @swc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@swc.errors.count, "error") %> prohibited this swc from being saved:</h2>

      <ul>
      <% @swc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
