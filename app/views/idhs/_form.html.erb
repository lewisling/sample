<%= form_for(@idh) do |f| %>
  <% if @idh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idh.errors.count, "error") %> prohibited this idh from being saved:</h2>

      <ul>
      <% @idh.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
